{
  "name": "lock-inc",
  "version": "1.2.0",
  "description": "Simple lock based on redis incr command",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "test-coverage": "istanbul cover node_modules/.bin/_mocha -- -R landing",
    "coverage-report": "export COVERALLS_GIT_COMMIT=`git rev-parse HEAD` && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "lock",
    "sync",
    "ipc"
  ],
  "author": "Anatoliy Chakkaev",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^2.4.0",
    "mocha": "^2.4.5",
    "redis": "^2.5.2",
    "should": "^8.2.2"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "redis": "^2.5.2"
  }
}
